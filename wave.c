#include <stdio.h>

int main(){
    int arr[]={5,10,4,3,1};
     int n=sizeof(arr)/sizeof(int);
     for (int st=0;st<n;st++){
        for(int en=st+1;en<n;en++){
            int currSum=0;
            for(int k=st;k<en;k++)
        }
     }
} 